name: DB bootstrap production (manual)

on:
  workflow_dispatch:

concurrency:
  group: release-production
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  bootstrap:
    uses: ./.github/workflows/db-bootstrap.yml
    with:
      environment: prod
    secrets: inherit
