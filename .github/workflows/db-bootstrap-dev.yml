name: DB bootstrap dev (manual)

on:
  workflow_dispatch:

concurrency:
  group: release-dev
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  bootstrap:
    uses: ./.github/workflows/db-bootstrap.yml
    with:
      environment: dev
    secrets: inherit
