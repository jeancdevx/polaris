name: Deploy web-admin production (manual)

on:
  workflow_dispatch:

concurrency:
  group: release-production
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-web-admin.yml
    with:
      environment: prod
    secrets: inherit
