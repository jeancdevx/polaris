name: DB reset dev

# Destructivo: TRUNCATE tablas de app + seed + flush/sync Redis en dev.
# Solo manual — no corre en push.
on:
  workflow_dispatch:
    inputs:
      confirm:
        description:
          'Escribe reset-dev para confirmar (borra datos de RDS dev y recarga
          seed)'
        required: true
        type: string

concurrency:
  group: db-reset-dev
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

env:
  PROJECT_NAME: polaris
  AWS_REGION: ${{ vars.AWS_REGION || 'us-east-2' }}

jobs:
  reset:
    name: Reset dev database and Redis
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: dev
    if: inputs.confirm == 'reset-dev'
    env:
      NAME_PREFIX: polaris-dev
    steps:
      - name: Validate confirmation
        run: |
          if [ "${{ inputs.confirm }}" != "reset-dev" ]; then
            echo "::error::Confirmation must be exactly reset-dev"
            exit 1
          fi

      - uses: actions/checkout@v7

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push db-bootstrap image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: apps/db-bootstrap/Dockerfile
          push: true
          tags: |
            ${{ steps.ecr.outputs.registry }}/${{ env.NAME_PREFIX }}-db-bootstrap:${{ github.sha }}
            ${{ steps.ecr.outputs.registry }}/${{ env.NAME_PREFIX }}-db-bootstrap:latest
          cache-from: type=gha,scope=db-bootstrap
          cache-to: type=gha,scope=db-bootstrap,mode=max

      - name: Register task definition with new image
        id: task
        run: |
          FAMILY="${NAME_PREFIX}-db-bootstrap"
          IMAGE="${{ steps.ecr.outputs.registry }}/${NAME_PREFIX}-db-bootstrap:${{ github.sha }}"

          CURRENT=$(aws ecs describe-task-definition \
            --task-definition "$FAMILY" \
            --query 'taskDefinition' \
            --output json)

          NEW_DEF=$(echo "$CURRENT" | jq \
            --arg IMAGE "$IMAGE" \
            '.containerDefinitions[0].image = $IMAGE
             | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)')

          REV=$(aws ecs register-task-definition \
            --cli-input-json "$NEW_DEF" \
            --query 'taskDefinition.revision' \
            --output text)

          echo "family=$FAMILY" >> "$GITHUB_OUTPUT"
          echo "revision=$REV" >> "$GITHUB_OUTPUT"
          echo "Registered ${FAMILY}:${REV} with image ${IMAGE}"

      - name: Run db-reset ECS task
        id: run
        run: |
          DATABASE_URL="$(bash scripts/build-database-url-from-rds.sh "${NAME_PREFIX}")"
          echo "::add-mask::$DATABASE_URL"

          SUBNETS=$(aws ec2 describe-subnets \
            --filters "Name=tag:Name,Values=${NAME_PREFIX}-private-*" \
            --query 'Subnets[*].SubnetId' \
            --output text | tr '\t' ',')

          SG=$(aws ec2 describe-security-groups \
            --filters "Name=tag:Name,Values=${NAME_PREFIX}-ecs-sg" \
            --query 'SecurityGroups[0].GroupId' \
            --output text)

          OVERRIDES=$(jq -n \
            --arg url "$DATABASE_URL" \
            --arg cmd0 "node" \
            --arg cmd1 "dist/reset.js" \
            '{containerOverrides:[{name:"db-bootstrap",command:[$cmd0,$cmd1],environment:[{name:"DATABASE_URL",value:$url}]}]}')

          TASK_ARN=$(aws ecs run-task \
            --cluster "${NAME_PREFIX}-cluster" \
            --task-definition "${{ steps.task.outputs.family }}:${{ steps.task.outputs.revision }}" \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[${SUBNETS}],securityGroups=[${SG}],assignPublicIp=DISABLED}" \
            --overrides "$OVERRIDES" \
            --query 'tasks[0].taskArn' \
            --output text)

          echo "task_arn=$TASK_ARN" >> "$GITHUB_OUTPUT"
          echo "Started reset task: $TASK_ARN"

      - name: Wait for task completion
        run: |
          aws ecs wait tasks-stopped \
            --cluster "${NAME_PREFIX}-cluster" \
            --tasks "${{ steps.run.outputs.task_arn }}"

          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster "${NAME_PREFIX}-cluster" \
            --tasks "${{ steps.run.outputs.task_arn }}" \
            --query 'tasks[0].containers[0].exitCode' \
            --output text)

          STOP_REASON=$(aws ecs describe-tasks \
            --cluster "${NAME_PREFIX}-cluster" \
            --tasks "${{ steps.run.outputs.task_arn }}" \
            --query 'tasks[0].stoppedReason' \
            --output text)

          echo "Exit code: $EXIT_CODE"
          echo "Stop reason: $STOP_REASON"

          if [ "$EXIT_CODE" != "0" ]; then
            echo "::error::db-reset task failed (exit $EXIT_CODE)"
            exit 1
          fi

          echo "Dev reset completed: TRUNCATE + seed + Redis flush/sync"

      - name: Tail CloudWatch logs on failure
        if: failure()
        run: |
          LOG_GROUP="/ecs/${NAME_PREFIX}-db-bootstrap"
          STREAM=$(aws logs describe-log-streams \
            --log-group-name "$LOG_GROUP" \
            --order-by LastEventTime \
            --descending \
            --max-items 1 \
            --query 'logStreams[0].logStreamName' \
            --output text)

          if [ "$STREAM" != "None" ] && [ -n "$STREAM" ]; then
            aws logs get-log-events \
              --log-group-name "$LOG_GROUP" \
              --log-stream-name "$STREAM" \
              --limit 80 \
              --query 'events[*].message' \
              --output text
          fi

  rejected:
    name: Confirmation required
    runs-on: ubuntu-latest
    if: inputs.confirm != 'reset-dev'
    steps:
      - run: |
          echo "::error::Workflow cancelled: type reset-dev in the confirm input"
          exit 1
